# poweredge secrets — encrypt with: sops secrets/poweredge/secrets.yaml
# After first deploy, re-encrypt with poweredge's SSH host key:
#   ssh root@<ip> cat /etc/ssh/ssh_host_ed25519_key.pub | ssh-to-age
# Then update .sops.yaml and run: sops updatekeys -y secrets/poweredge/secrets.yaml
nextcloud/admin-password: REPLACE_ME
tailscale/auth-key: tskey-auth-REPLACE_ME
